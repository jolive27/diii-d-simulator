Each physics change request must identify assumption IDs, old and proposed values/equations, units, reason, affected tests, compatibility implications, new model version, Physics agent identity, and Director decision. Implement only after approval; retain both previous and new registry versions in Git. Numerical instrumentation without changing equations must explicitly say no assumptions changed.
