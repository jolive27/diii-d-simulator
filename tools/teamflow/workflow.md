# TeamFlow — workflow through agents

![TeamFlow workflow](workflow.png)

> Static render (2× PNG). Source: `workflow.mmd`. Regenerate with `node tools/teamflow/render-workflow.mjs` (mermaid v11 + headless Chrome) — no mermaid preview extension needed.